# DrfSttProject

This is the complete backend of my Speech to text aplication with AI editing, Transcripting and many other features

To run the project you must create virtual environment and install all the modules inside the requirements.txt, file after that you cd into the core project and run "python manage.py runserver", then you must start redis server and start celery worker
